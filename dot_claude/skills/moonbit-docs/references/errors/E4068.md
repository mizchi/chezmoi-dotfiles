---
title: "E4068 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4068"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4068.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4068.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4068.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4068.md "Download source file")
* .pdf

# E4068

## Contents

# E4068[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4068.html#e4068 "Link to this heading")

Main function is already defined.

In MoonBit, a `main` package contains a single `main` function that serves as
the entry point of the program. Therefore, you cannot define more than one
`main` function in the main package. If you want to have multiple programs to be
built as separate binaries, you can use multiple `main` packages within the same
module.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4068.html#erroneous-example "Link to this heading")

Suppose in package `main`:

`main/moon.pkg.json`:

```
{
  "is-main": true
}
```

`main/main.mbt`:

```
fn main {
  println("Hello, World!")
}

fn main { // Error: Main function is already defined at ...
  println("Hello, World! Again!")
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4068.html#suggestion "Link to this heading")

You can either remove the extra `main` function:

```
fn main {
  println("Hello, World!")
}
// Remove the extra main function
//- fn main {
//-   println("Hello, World! Again!")
//- }
```

Or you can move the extra `main` function to a different package, say `main2`:

`main2/moon.pkg.json`:

```
{
  "is-main": true
}
```

`main2/main.mbt`:

```
fn main {
  println("Hello, World! Again!")
}
```

while keeping the `main/main.mbt` contains only one main function:

```
fn main {
  println("Hello, World!")
}
```

Contents
