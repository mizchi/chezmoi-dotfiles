---
title: "E2000 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E2000"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E2000.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E2000.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E2000.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E2000.md "Download source file")
* .pdf

# E2000

## Contents

# E2000[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#e2000 "Link to this heading")

The usage of function (type, trait, etc.) is flagged with alert. Usually, alert
message comes with a alert kind and a detailed description of the alert. If you
are using the function from a library, these alerts are set by the library
author to provide some more information on the usage of the function

There are some common alerts that you may encounter:

* `deprecated`: indicates the function (or type, trait, etc.) is deprecated and
  should not be used. You should migrate to new APIs.
* `unsafe`: indicates this API may panic, have internal invariants, or have undefined
  behavior under some circumstances. The concrete semantics of this kind of
  alerts may be different across packages, and please consult the documentation
  or the author of these packages for further details.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#erroneous-example "Link to this heading")

lib/top.mbt[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#id1 "Link to this code")

```
///|
#deprecated("Use `greet` instead")
pub fn greeting() -> String {
  "Hello!"
}

///|
pub fn greet(name~ : String = "") -> String {
  if name != "" {
    "Hello!"
  } else {
    "Hello, \{name}!"
  }
}
```

top.mbt[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#id2 "Link to this code")

```
///|
fn main {
  println(@lib.greeting())
  //      ^~~~~~~~ Warning (Alert deprecated): Use `greet` instead(2000)
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#suggestion "Link to this heading")

One way to fix the alert, is to change your code as suggested by the message (like `deprecated`):

top.mbt[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#id3 "Link to this code")

```
///|
fn main {
  println(@lib.greet())
}
```

If you clearly know what you are doing and would like to suppress the alert, you can change the `moon.pkg.json` file for packages where you would like to disable **this kind of alert**. For example:

moon.pkg.json[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E2000.html#id4 "Link to this code")

```
{
  "is-main": true,
  "import": [
    "moonbit-community/E2000-fixed/lib"
  ],
  "alert-list": "-deprecated"
}
```

Note

There is no way to disable alerts for a line/file.

Contents
