---
title: "E0018 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0018"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0018.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0018.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0018.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0018.md "Download source file")
* .pdf

# E0018

## Contents

# E0018[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0018.html#e0018 "Link to this heading")

There is no `continue` in this loop expression, so `loop` is useless here.

This error occurs when using a `loop` expression that contains no `continue`
statement. In MoonBit, a `loop` without any `continue` statements is equivalent
to an block expression that will be executed only once, making the `loop`
keyword redundant in such cases. You should either add a `continue` statement if
you need to restart the loop from the beginning, or use a simpler control flow
construct like `match` or `if`.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0018.html#erroneous-example "Link to this heading")

```
///|
fn main {
  let count = 2
  loop count {
    _..<0 => break
    _ => println(count)
  }
}
```

Output:

```
2
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0018.html#suggestion "Link to this heading")

Either add a `continue` statement if you need to restart the loop:

```
///|
fn main {
  let count = 2
  loop count {
    _..<0 => break
    count => {
      println(count)
      continue count - 1
    }
  }
}
```

Output:

```
2
1
0
```

Or use `if`/`match` you don't need to use `continue`:

```
///|
fn main {
  let count = 2
  match count {
    _..<0 => ()
    _ => println(count)
  }
}
```

Output:

```
2
```

Contents
