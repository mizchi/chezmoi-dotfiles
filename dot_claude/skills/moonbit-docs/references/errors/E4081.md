---
title: "E4081 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4081"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4081.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4081.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4081.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4081.md "Download source file")
* .pdf

# E4081

## Contents

# E4081[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4081.html#e4081 "Link to this heading")

The identifier is bound more than once in the same pattern.

It is not possible to bind to values into one identifier because they might have
different values. If you want to shadow the first identifier, you can use `_` to
discard it.

## Erroneous Example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4081.html#erroneous-example "Link to this heading")

```
fn f(a : Int?, b : Int?) -> Unit {
  match (a, b) {
    (Some(a), Some(a)) => println("Some(\{a})")
    //    ^ Error: The identifier a is bound more than once in the same pattern.
    _ => println("None")
  }
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4081.html#suggestion "Link to this heading")

Use a different name for the second identifier.

```
fn f(a : Int?, b : Int?) -> Unit {
  match (a, b) {
    (Some(a), Some(b)) => println("Some(\{a}), Some(\{b})")
    _ => println("None")
  }
}
```

If you want a shadow-like behavior here, you can explicitly discard the first
`a` using `_`:

```
fn f(a : Int?, b : Int?) -> Unit {
  match (a, b) {
    (Some(_), Some(a)) => println("Some(\{a})")
    _ => println("None")
  }
}
```

Contents
