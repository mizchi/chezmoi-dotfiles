---
title: "E0011 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0011"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0011.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0011.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0011.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0011.md "Download source file")
* .pdf

# E0011

## Contents

# E0011[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0011.html#e0011 "Link to this heading")

Partial match. The match/guard/loop expression does not cover all possible
cases.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0011.html#erroneous-example "Link to this heading")

```
///|
fn main {
  match Some(1) { // Partial match, some hints: None
    Some(x) => println(x)
  }
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0011.html#suggestion "Link to this heading")

The warning message usually contains hints about the missing patterns. Add the missing cases to avoid incomplete matches.
You can use [quick fix](https://code.visualstudio.com/docs/editing/refactoring#_code-actions-quick-fixes-and-refactorings)
when using [VSCode Plugin](https://docs.moonbitlang.com/en/stable/toolchain/vscode/index.html#actions).

```
///|
fn main {
  match Some(1) {
    Some(x) => println(x)
    None => println("None")
  }
}
```

Or, you can use `is` syntax to use this pattern matching as a condition:

```
///|
/// Example using `is` syntax for conditional pattern matching
pub fn example_with_is() -> Unit {
  if Some(1) is Some(x) {
    println(x)
  }
}
```

Or, you can add a wildcard pattern to catch all remaining cases:

```
///|
/// Example using wildcard pattern to catch all remaining cases
pub fn example_with_wildcard() -> Unit {
  match Some(1) {
    Some(x) => println(x)
    _ => println("Other")
  }
}
```

Contents
