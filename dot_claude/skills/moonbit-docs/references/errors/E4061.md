---
title: "E4061 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4061"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4061.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4061.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4061.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4061.md "Download source file")
* .pdf

# E4061

## Contents

# E4061[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4061.html#e4061 "Link to this heading")

Cannot implement a trait for a type when both are defined in other packages.

MoonBit follows the orphan rule, which means that you can only:

| Traits | Types | Allowed? |
| --- | --- | --- |
| Current Package | Current Package | Yes |
| Other Package | Current Package | Yes |
| Current Package | Other Package | Yes |
| Other Package | Other Package | No |

See the [Access control of methods and trait implementations](https://docs.moonbitlang.com/en/stable/language/packages.html#trait-implementations)
section of the MoonBit documentation for more information.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4061.html#erroneous-example "Link to this heading")

Suppose you have type `A` and trait `B` defined in package `a` in module
`username/hello`:

`a/a.mbt`:

```
pub(all) type A Int
pub(open) trait B {
  to_int(Self) -> Int
}
```

`a/moon.pkg.json`:

```
{}
```

Now, if you want to implement trait `@a.B` for type `@a.A`:

`b/moon.pkg.json`:

```
{
  "import": [
    "username/hello/a"
  ]
}
```

`b/b.mbt`:

```
impl @a.B for @a.A with to_int(self : @a.A) -> Int {
  //                    ^~~~~~
  // Error: Cannot define method to_int of trait @a.B from package a for type @a.A from package a
  self._
}
```

MoonBit will report an error.

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4061.html#suggestion "Link to this heading")

You can move either the trait or type into the current package where the
implementation is in. If that is not possible, you can create a new type over
the type you wish to implement the trait for, and then implement the trait for
the new type:

`b/b.mbt`:

```
type WrapA @a.A

impl @a.B for WrapA with to_int(self : WrapA) -> Int {
  ...
}
```

Contents
