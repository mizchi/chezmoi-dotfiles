---
title: "E0004 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0004"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0004.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0004.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0004.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0004.md "Download source file")
* .pdf

# E0004

## Contents

# E0004[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0004.html#e0004 "Link to this heading")

The abstract type does not occur in public signature of current package,
consider marking it as `priv`.

If an abstract type is not used elsewhere in the public interface of a package,
then it is not possible for user to interact with the value of that type in any
way. Therefore, it is highly probable that the type is part of the private
implementation of the package. In this case, it is better to mark the type as
`priv` to indicate that it is not part of the public interface of the package.

It is also possible that, this abstract type is part of the public interface of
the package, but relevant functions, methods, or types are not marked as `pub`.

This warning is not emitted inside `main` packages which are usually not
intended to be used by others anyway. Likewise, it is not emitted inside
[whitebox test and blackbox test](https://docs.moonbitlang.com/en/stable/language/tests.html#blackbox-tests-and-whitebox-tests)
files.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0004.html#erroneous-example "Link to this heading")

```
///|
struct Code(Int)

///|
fn Code::new(code : Int) -> Code {
  Code(code)
}

///|
test {
  let Code(_) = Code::new(42)

}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0004.html#suggestion "Link to this heading")

If the abstract type is not part of the public interface of the package, then it
should be marked as `priv`:

```
///|
priv struct Code(Int)
```

Or, if the abstract type is part of the public interface of the package, then
make relevant definitions `pub`:

```
///|
struct Code2(Int) derive(Show)

///|
pub fn Code2::new(value : Int) -> Code2 {
  return value
}
```

Contents
