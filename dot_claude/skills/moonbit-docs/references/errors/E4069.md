---
title: "E4069 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4069"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4069.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4069.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4069.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4069.md "Download source file")
* .pdf

# E4069

## Contents

# E4069[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4069.html#e4069 "Link to this heading")

Unexpected main function in the non-main package.

In MoonBit, packages are divided into two types: main packages and non-main
packages. Non-main packages are often used as libraries to provide functionality
to other packages. Main packages, on the other hand, are the entry points of the
program. Therefore, non-main packages should not contain a `main` function.

Sometimes, people use the main function for testing. In such cases, we recommend
using the `test` block to write tests, instead of using the `main` function. See
the [test](https://docs.moonbitlang.com/en/stable/language/introduction.html#test) section of the MoonBit documentation for
more information.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4069.html#erroneous-example "Link to this heading")

Suppose in package `lib`:

`lib/moon.pkg.json`:

```
{} // "is-main" is by default false
```

`lib/lib.mbt`:

```
fn main { // Error: Unexpected main function in the non-main package.
  println("Hello, world!")
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4069.html#suggestion "Link to this heading")

You can remove the `main` function from the non-main package:

```
// Remove the main function
//- fn main {
//-   println("Hello, world!")
//- }
```

Alternatively, you can set the package to be a main package by setting
`"is-main"` to `true` in the package configuration file.

`lib/moon.pkg.json`:

```
{
  "is-main": true
}
```

You can also move the `main` function to a different package, say `main`:

`main/moon.pkg.json`:

```
{
  "is-main": true
}
```

`main/main.mbt`:

```
fn main {
  println("Hello, world!")
}
```

Contents
