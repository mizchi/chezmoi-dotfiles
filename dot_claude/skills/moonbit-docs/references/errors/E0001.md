---
title: "E0001 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0001"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0001.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0001.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0001.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0001.md "Download source file")
* .pdf

# E0001

## Contents

# E0001[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0001.html#e0001 "Link to this heading")

Unused function.

This function is not used by any other part of your code, nor marked with `pub`
visibility.

Note that this warning might uncover other bugs in your code. For example, if
there are two functions in your codebase that has similar name, you might just
call the other function by mistake.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0001.html#erroneous-example "Link to this heading")

```
///|
fn greeting() -> String {
  "Hello!"
}

///|
test {
  fn local_greeting() -> String {
    "Hello, world!"
  }

}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0001.html#suggestion "Link to this heading")

There are multiple ways to fix this warning:

* If the function is indeed useless, you can remove the definition of the
  function.
* If this function is at the toplevel (i.e., not local), and is part of the
  public API of your module, you can add the `pub` keyword to the function.

  ```
  ///|
  pub fn greeting() -> String {
    "Hello!"
  }
  ```
* If you made a typo in the function name, you can rename the function to the
  correct name at the call site.

There are some cases where you might want to keep the function private and
unused at the same time. In this case, you can call `ignore()` on the function
to force the use of it.

```
///|
fn unused() -> String {
  "Hello, world!"
}

///|
test {
  ignore(unused)
}

///|
test {
  fn local_greeting() -> String {
    "Hello, world!"
  }

  ignore(local_greeting)
}
```

Contents
