---
title: "E4130 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4130"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4130.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4130.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4130.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4130.md "Download source file")
* .pdf

# E4130

## Contents

# E4130[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4130.html#e4130 "Link to this heading")

`derive` is not allowed for type alias.

Aliases of a type can be used interchangeably with the type itself, so it is not
possible to derive traits for an alias without also deriving them for the type
it aliases. Therefore, MoonBit disallows deriving traits for type aliases.
Instead, one should derive the traits for the type itself.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4130.html#erroneous-example "Link to this heading")

```
struct MyStruct {
  field: Int
}

typealias StructAlias = MyStruct derive(Show) // Error: `derive` is not allowed for type alias
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4130.html#suggestion "Link to this heading")

Move the `derive` attribute to the type itself:

```
struct MyStruct {
  field: Int
} derive(Show)

typealias StructAlias = MyStruct // Remove `derive`
```

If you do not have control over the type, you can create a new type that wraps
the original type. However, deriving traits for a new type requires the wrapped
type to also implement the trait, which is likely not the case if you were using
a type alias in the first place. In this case, you will need to implement the
trait manually.

```
type StructWrapper MyStruct

impl Show for StructWrapper with output(self, logger) {
  ...
}
```

Contents
