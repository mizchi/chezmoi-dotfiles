---
title: "E4020 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4020"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4020.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4020.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4020.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4020.md "Download source file")
* .pdf

# E4020

## Contents

# E4020[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4020.html#e4020 "Link to this heading")

Package not found in the loaded packages.

## Erroneous Example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4020.html#erroneous-example "Link to this heading")

```
let a = @boolean.to_int(true)
```

The example above tries to call the method `to_int` in a package `boolean`,
but this package is not present in the list of loaded packages,
giving the following error on line 1:

```
Package "boolean" not found in the loaded packages.
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4020.html#suggestion "Link to this heading")

Make sure the package name is valid.

If it is not a package in module `moonbitlang/core`, make sure that it is
mentioned as one of the imported packages in `moon.pkg.json` as well.

In the above example, the package name should be `@bool` instead of `@boolean`:

```
let a = @bool.to_int(true)
```

Contents
