---
title: "E4017 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4017"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4017.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4017.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4017.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4017.md "Download source file")
* .pdf

# E4017

## Contents

# E4017[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4017.html#e4017 "Link to this heading")

Method of type is ambiguous, it may come from multiple traits.

## Erroneous Example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4017.html#erroneous-example "Link to this heading")

```
struct S { v : Int } derive(Show)

trait Tee { to_string(Self) -> String }

impl Tee for S with to_string(_self) { "Tee" }

let s : S = { v: 3 }
let t = s.to_string()
```

The example above tries to call the method `to_string` on a type `S`,
but the method name comes from both `Show` and `Tee` traits,
giving the following error on line 8:

```
Method to_string of type S is ambiguous, it may come from trait Tee or Show
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4017.html#suggestion "Link to this heading")

Disambiguate the method by specifying the trait it comes from:

```
struct S { v : Int } derive(Show)

trait Tee { to_string(Self) -> String }

impl Tee for S with to_string(_self) { "Tee" }

let s : S = { v: 3 }
let t = Tee::to_string(s)
```

Contents
