---
title: "E4094 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4094"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4094.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4094.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4094.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4094.md "Download source file")
* .pdf

# E4094

## Contents

# E4094[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4094.html#e4094 "Link to this heading")

Cannot modify a read-only field.

For a read-only `struct`, you cannot modify the value of a field using
assignment outside of the package where the type is defined.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4094.html#erroneous-example "Link to this heading")

Suppose you have a package `a` in module `username/hello`:

`a/moon.pkg.json`:

```
{}
```

`a/a.mbt`:

```
pub struct T {
  mut value : Int
}

pub fn T::new() -> T {
  T::{ value: 0 }
}

pub fn T::set(self : T, value : Int) -> Unit {
  self.value = value
}
```

And you have another package `b` in the same module:

`b/moon.pkg.json`:

```
{
  "is-main": true,
  "import": [
    "username/hello/a"
  ]
}
```

`b/b.mbt`:

```
fn main {
  let a = @a.T::new()
  a.value = 3 // Error: Cannot modify a read-only field: value
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4094.html#suggestion "Link to this heading")

If you have control over the package where the type is defined, you can simply
make the struct has `pub(all)` visibility.

`a/a.mbt

```
pub(all) struct T {
  mut value : Int
}
```

However, this might not be the best solution if you want to hide the
implementation details of the struct or protect the invariant of the struct. We
recommend using getter/setter pattern to guard the access to the struct fields.

`b/b.mbt`:

```
fn main {
  let a = @a.T::new()
  a.set(3)
}
```

Contents
