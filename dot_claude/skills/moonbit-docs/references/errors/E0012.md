---
title: "E0012 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0012"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0012.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0012.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0012.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0012.md "Download source file")
* .pdf

# E0012

## Contents

# E0012[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0012.html#e0012 "Link to this heading")

Unreachable code. This usually happens when a `return` statement is followed by
more code. The `return` statement will exit the function immediately, so any
code after it will not be executed.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0012.html#erroneous-example "Link to this heading")

```
///|
/// Example function showing unreachable code after an early return
fn unreachable_early_return() -> Unit {
  return
  println("Hello, World!") // Warning: Unreachable code
}
```

```
///|
// Example function showing unreachable pattern matching
fn unreachable_match_pattern() -> Unit {
  match Some(1) {
    _ => println("_")
    Some(a) => println("Some(\{a})") // Warning: Unreachable code
  }
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0012.html#suggestion "Link to this heading")

Remove the unreachable code. Or adjust the branching statements.

```
///|
fn example_execute_before_return() -> Unit {
  println("Hello World!")
  return
}
```

You can also swap the order of the branches in the pattern matching. If the
order of the branches is important, then you may want to refine the first
pattern so that it excludes what the second pattern covers.

```
///|
fn example_proper_order() -> Unit {
  match Some(1) {
    Some(a) => println("Some(\{a})")
    _ => println("_")
  }
}
```

Or,

```
///|
fn example_specific_pattern() -> Unit {
  match Some(1) {
    None => println("_")
    Some(a) => println("Some(\{a})")
  }
}
```

Contents
