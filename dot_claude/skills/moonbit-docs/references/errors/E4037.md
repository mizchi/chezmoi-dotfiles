---
title: "E4037 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4037"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4037.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4037.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4037.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4037.md "Download source file")
* .pdf

# E4037

## Contents

# E4037[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4037.html#e4037 "Link to this heading")

Cannot perform action: package is not imported.

## Erroneous Example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4037.html#erroneous-example "Link to this heading")

In `main/main.mbt`, we try to call a function from `@middle`:

```
fn main {
  let a = @middle.return_Obj().m()
}
```

In `middle/middle.mbt`, we define a function that returns an
object of a trait `Obj` defined in `@ty`:

```
pub fn return_Obj() -> &@ty.Obj {
  @ty.T(42)
}
```

In `ty/ty.mbt`, we define a trait `Obj` and a type `T` that
implements it:

```
pub(all) type T Int

pub fn f(self: T) -> Unit {
  println(self._)
}

pub(open) trait Obj {
  f(Self) -> Unit
}
```

... but in `main/moon.pkg.json` there is no mention of `@ty`:

```
{
  "import": ["<PACKAGE>/middle"]
}
```

This gives the following error on line 2 of `main/main.mbt`:

```
Cannot call method of type &@<PACKAGE>/ty.Obj: package @<PACKAGE>/ty is not imported.
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4037.html#suggestion "Link to this heading")

Make sure that all relevant packages are imported.

In the above example, this means importing both `@middle` and `@ty` in `main/moon.pkg.json`:

```
{
  "import": ["<PACKAGE>/middle", "<PACKAGE>/ty"]
}
```

Contents
