---
title: "E4120 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4120"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4120.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4120.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4120.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4120.md "Download source file")
* .pdf

# E4120

## Contents

# E4120[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4120.html#e4120 "Link to this heading")

The application might raise errors, but it's not handled. Try adding an infix
operator `!` or `?` to the application.

In MoonBit, we require programmers to explicitly annotate which functions may
raise errors. This is done by adding an infix operator `!` or `?` to the
function application. The `!` operator to re-raise the error, and the `?`
operator is to materialize the error to a `Result[T, E]` type.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4120.html#erroneous-example "Link to this heading")

```
fn may_raise_error(input : Int) -> Unit! {
  if input == 42 {
    return
  }
  fail!("failed")
}

fn main {
  may_raise_error(42)
  // Error: The application might raise errors of type Error, but it's not handled.
  // Try adding a infix operator `!` or `?` to the application, so that it looks like `...!(...)` or `...?(...)`.
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4120.html#suggestion "Link to this heading")

You can either re-raise the error:

```
fn main {
  try {
    may_raise_error!(42)
  } catch {
    error => println("Error: \{error}")
  }
}
```

Or materialize the error to a `Result[T, E]` type:

```
fn main {
  let result = may_raise_error?(42)
  println("Result: \{result}")
}
```

Contents
