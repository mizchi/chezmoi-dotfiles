---
title: "E0003 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0003"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0003.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0003.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0003.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0003.md "Download source file")
* .pdf

# E0003

## Contents

# E0003[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0003.html#e0003 "Link to this heading")

Unused type declaration.

This type is not used by any other part of your code, which means it's not
constructed nor consumed, and others can't construct nor consume due to
visibility. Type declaration is by default abstract, which means it is publicly
visible to users of this package, but they cannot construct nor destruct the
type. Therefore, this warning is only shown if the type has abstract or private
visibility, and is not constructed nor consumed by any other part of this
package.

Note that this warning might uncover other bugs in your code. For example, if
there are two types in the package that has similar name, you might just use the
other type by mistake.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0003.html#erroneous-example "Link to this heading")

```
///|
struct Foo {
  bar : Int
}

///|
pub fn f() -> Unit {
  struct Bar {
    foot : Int
  }

}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0003.html#suggestion "Link to this heading")

There are multiple ways to fix this warning:

* If the type is indeed useless, you can remove the definition of the type.
* If this type is not local, and is part of the public API of your module, you
  can add `pub` or `pub(all)` visibility keyword from the type.

  ```
  ///|
  pub struct Foo {
    bar : Int
  }
  ```
* Check if you are referencing the type with a correct name.

It is rather rare to have a type that is private and unused at the same time;
therefore, we suggest you remove the type if it is not used.

Contents
