---
title: "E4087 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4087"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4087.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4087.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4087.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4087.md "Download source file")
* .pdf

# E4087

## Contents

# E4087[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4087.html#e4087 "Link to this heading")

Mutating something immutable, such as variable or struct field.

MoonBit requires programmers to explicitly declare mutable variables and struct fields.
Notice that in MoonBit, mutability of variable only refers to the variable itself,
not the value it holds.
So when you declare an immutable `let` value of type `Array[T]`, `Ref[T]`, and `struct` with mutable field,
you can still mutate the inner structure, but cannot replace the value of the whole variable.

Tuples are always immutable. If you want mutability, use a `struct` with mutable field instead.

## Erroneous Example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4087.html#erroneous-example "Link to this heading")

```
test {
  let a = @ref.new(0)
  a = @ref.new(1) // Error: The variable a is not mutable.
  a.val = 1 // This is valid, because the mutability come from `Ref` type itself
  println(a)
}

struct S {
  x : Int
}

test {
  let s = { x: 1 }
  s.x = 2 // Error: The field `x` is not mutable.
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4087.html#suggestion "Link to this heading")

To fix this error, you need to declare the variable as mutable by adding the
`mut` keyword before the variable name or struct field name.

```
test {
  let mut a = 0
  a = 1
  println(a)
}

struct S {
  mut x : Int
//^^^
}

test {
  let s = { x: 1 }
  s.x = 2
}
```

Contents
