---
title: "E0041 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E0041"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E0041.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E0041.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E0041.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E0041.md "Download source file")
* .pdf

# E0041

## Contents

# E0041[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0041.html#e0041 "Link to this heading")

Closed map pattern.

MoonBit's map pattern syntax does not support closed pattern
(i.e. asserting that the map does not contain any other unmatched elements).
So map patterns must always contain a `..`, otherwise this warning will be reported.

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0041.html#erroneous-example "Link to this heading")

```
///|
fn f(map : Map[String, Int]) -> Bool {
  match map {
    // the pattern still matches
    // if `map` contain elements other than "x" and "y"
    { "x": x, "y": y } => true
    _ => false
  }
}

///|
test {
  inspect(f({ "x": 1, "y": 2, "z": 3 }), content="true")
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E0041.html#suggestion "Link to this heading")

* If the intended semantic is open matching (i.e. allow existence of unmatched elements),
  just add `..` to the map pattern.
* If the intended semantic is closed matching, use pattern guard instead:

  ```
  ///|
  fn f(map : Map[String, Int]) -> Bool {
    match map {
      { "x": x, "y": y, .. } if map.length() == 2 => true
      _ => false
    }
  }

  ///|
  test {
    inspect(f({ "x": 1, "y": 2, "z": 3 }), content="false")
  }
  ```

Contents
