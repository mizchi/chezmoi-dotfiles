---
title: "E4053 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4053"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4053.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4053.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4053.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4053.md "Download source file")
* .pdf

# E4053

## Contents

# E4053[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4053.html#e4053 "Link to this heading")

Invalid type for "self": must be a type constructor.

This error happens when you want to define a method for implement a trait for a
type that is not a type constructor.

Types that **are** type constructors:

* Tuple: `(Int, Bool)`
* `enum`s, `struct`s, `trait`s, new types (`type`), and error types (`type!`).

Types that **are not** type constructors:

* Function: `(Int) -> Bool`
* Type parameter: `T` in `fn f[T](https://docs.moonbitlang.com/en/stable/language/error_codes/x : T) -> T`

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4053.html#erroneous-example "Link to this heading")

* Defining a method for a type that is not type constructor.

  ```
  fn f[T](https://docs.moonbitlang.com/en/stable/language/error_codes/self : T) -> Int { // Error: Invalid type for "self": must be a type constructor.
    ignore(self)
    0
  }

  fn g(self : (Int) -> Unit) -> Unit { // Error: Invalid type for "self": must be a type constructor.
    self(0)
  }
  ```
* Implementing a method for a type that is not a type constructor.

  ```
  pub trait A {
    f(Self) -> Int
  }

  // Without constraints
  impl[X] A for X with f(self : X) -> Int { // Error: Invalid type for "self": must be a type constructor.
    ignore(self)
    0
  }

  // With constraints
  impl[X: Default] A for X with f(self : X) -> Int { // Error: Invalid type for "self": must be a type constructor.
    ignore(self)
    0
  }
  ```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4053.html#suggestion "Link to this heading")

When you want to define a method for a type that is not a type constructor, you
may use new type to wrap the type.

```
pub(all) type Wrap[T] T

fn f[T](https://docs.moonbitlang.com/en/stable/language/error_codes/self : Wrap[T]) -> Int {
  ignore(self)
  0
}

fn g(self : Wrap[(Int) -> Unit]) -> Unit {
  (self._)(0)
}

impl[X] A for Wrap[X] with f(self : Wrap[X]) -> Int {
  ignore(self)
  0
}
```

However, this means you may have to wrap the value with `Wrap` every time you
want to call the method.

If you find yourself implementing a trait for a type parameter, you may also
want to consider using default implementation for the trait.

```
impl A with f(self : Self) -> Int {
  ignore(self)
  0
}
```

If you want to provide the implementation for types with specific constraints,
you can add these constraints to the trait definition.

```
trait A: Default {
  f(Self) -> Int
}

impl A with f(self : Self) -> Int {
  ignore(self)
  0
}
```

Contents
