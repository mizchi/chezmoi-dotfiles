---
title: "E4101 — MoonBit v0.6.27 documentation"
source_url: "https://docs.moonbitlang.com/en/stable/language/error_codes/E4101"
fetched_at: "2025-12-19T08:15:11.537167+00:00"
---



* [Show source](https://github.com/moonbitlang/moonbit-docs/blob/main/next/language/error_codes/E4101.md?plain=1 "Show source")
* [Suggest edit](https://github.com/moonbitlang/moonbit-docs/edit/main/next/language/error_codes/E4101.md "Suggest edit")
* [Open issue](https://github.com/moonbitlang/moonbit-docs/issues/new?title=Issue%20on%20page%20%2Flanguage/error_codes/E4101.html&body=Your%20issue%20content%20here. "Open an issue")

* [.md](https://docs.moonbitlang.com/en/stable/_sources/language/error_codes/E4101.md "Download source file")
* .pdf

# E4101

## Contents

# E4101[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4101.html#e4101 "Link to this heading")

Unsupported expression after the pipe operator.

This error occurs when the expression after the pipe operator (`|>`) is not in a
supported form. The pipe operator allows you to chain function calls in a more
readable way, but only supports specific forms on its right-hand side.

The following forms are allowed after the pipe operator:

1. A single identifier (function name)
2. A regular function application (but not method calls)
3. A constructor name
4. A constructor application

## Erroneous example[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4101.html#erroneous-example "Link to this heading")

```
type T Int

fn m(self: T, x: Int) -> Unit {
  println(self._ + x)
}

pub fn f(t: T, x: Int) -> Unit {
  x |> fn(x: Int) { println(x)}
//     ^^^^^^^^^^^^^^^^^^^^^^^^ Error: Unsupported expression after the pipe operator.
  x |> t.m()
//     ^^^^ Error: Unsupported expression after the pipe operator.
}
```

## Suggestion[#](https://docs.moonbitlang.com/en/stable/language/error_codes/E4101.html#suggestion "Link to this heading")

To fix this error, you can change the invalid pipe expression to normal function
or method application.

```
type T Int

fn m(self: T, x: Int) -> Unit {
  println(self._ + x)
}

pub fn f(t: T, x: Int) -> Unit {
  fn(x: Int) { println(x)}(x)
  t.m(x)
}
```

Contents
